<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content" >

    <!-- android:layout_width="@dimen/layoutwidth" -->
    <!-- android:layout_height="@dimen/layoutheight" -->
    <!-- <View -->
    <!-- android:id="@+id/rightView" -->
    <!-- android:layout_width="10dp" -->
    <!-- android:layout_height="fill_parent" -->
    <!-- android:layout_centerInParent="true" -->
    <!-- android:background="#DF3A01" /> -->

    <LinearLayout
        android:id="@+id/LinearLayoutLeft"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/chat_bg_right"
        android:orientation="vertical" >

        <ImageView
            android:id="@+id/imageView_chat_right"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:layout_gravity="top|center"
            android:layout_marginRight="7dp"
            android:adjustViewBounds="true"
            android:contentDescription="@string/app_name"
            android:padding="7dp"
            android:scaleType="fitXY"
            android:src="@drawable/no_image" />

        <TextView
            android:id="@+id/TextView_EventNameRight"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:ellipsize="marquee"
            android:fadingEdge="horizontal"
            android:lines="1"
            android:marqueeRepeatLimit="marquee_forever"
            android:scrollHorizontally="true"
            android:selectAllOnFocus="true"
            android:singleLine="true"
            android:text="The Big EventThe Big EventThe Big Event"
            android:textColor="@android:color/white" />
    </LinearLayout>

</RelativeLayout>